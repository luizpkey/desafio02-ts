import {
   Box,
} from '@chakra-ui/react'

export const ContainerApp = ({ children }: any) => {
   return (
      <Box minHeight='100vh' bgGradient='linear(to-b, #ffa500, #9413dc)' padding='0px'>{children}</Box>
           )
         }
